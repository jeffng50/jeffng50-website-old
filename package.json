{
  "name": "jeffng50-website",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "local-cms": "turbo run dev --filter=cms",
    "build-frontend": "turbo run build --filter=jeffng50-website-frontend",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "packageManager": "npm@10.8.1",
  "devDependencies": {
    "turbo": "^2.0.4"
  },
  "workspaces": [
    "cms",
    "frontend"
  ]
}
